Take these and make them into specs then remove the line

- thought chunks are missing a space between them.
- Syntax highlighting for code being read and written (json,markdown,typescript to start with).
- Make the scrollbar for the chat full width just the content should be centered and the same size as the input box.
- When pasting code snippets I want the displayed message to retain the newlines
- Fix the /command and @ so arrow keys up and down selects different command in the intilisene